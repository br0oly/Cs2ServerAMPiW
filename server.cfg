// ─── PODSTAWY ───────────────────────────────────────
hostname          "AMPiW CS2 2026 - CDV Poznan"
sv_password       ""              // brak hasła — gracze łączą się swobodnie przez IP
sv_cheats         0
sv_lan            0               // Zmień na 1 TYLKO jeśli całkowicie padnie internet w sali

// ─── RCON ───────────────────────────────────────────
rcon_password     "zaq1@WSX"   // Pamiętaj o zmianie na własne bezpieczne hasło

// ─── GOTV (Pod Live Hud Manager / Scout AI) ─────────
tv_enable         1
tv_port           27020           
tv_delay          0               // 0s delay dla natychmiastowego sczytywania danych przez HUD
tv_autorecord     1               // Backup demek w razie czego
tv_delaymapchange 1               // Zapobiega crashom widzów przy zmianie mapy
tv_advertise_watchable 1
tv_maxclients     5

// ─── GAMEPLAY BAZOWY (MatchZy i tak to nadpisze po loadmatch) ───
mp_freezetime     15
mp_round_restart_delay 5
mp_maxrounds      24              
mp_overtime_enable 1
mp_overtime_maxrounds 6 
mp_td_dmgtokick 0
mp_td_dmgtowarn 0

// ─── PERFORMANCE (CS2 WYMAGA TYLKO TEGO) ────────────
sv_minrate        786432          // Minimalny dopuszczalny rate (wartość dla dobrych łączy/LAN)
sv_maxrate        0               // Brak limitu pobierania danych z serwera
sv_maxroutable 1200
net_splitrate 4

// ─── LOGOWANIE POD LHM / SCOUT AI ───────────────────
log               on
logaddress_add    "192.168.1.20:27115"
